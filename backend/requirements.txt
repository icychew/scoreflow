# AI Pipeline
demucs>=4.0.0
basic-pitch>=0.3.0
music21>=9.1.0
pretty_midi>=0.2.10
soundfile>=0.12.0

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Utilities
pydantic>=2.5.0

# PDF rendering
verovio>=3.15.0
cairosvg>=2.7.0
pypdf>=4.0.0

# Tempo / beat detection
librosa>=0.10.0

# YouTube link ingestion
yt-dlp>=2024.1.0

# High Quality mode (optional — used by quality="high" only).
# Lazy-imported in separator.py; omitted from the default image to keep the
# HF Spaces free-tier Docker build under its timeout. Install manually to
# enable HQ separation: `pip install audio-separator>=0.23.0`
# audio-separator>=0.23.0
# piano-transcription-inference: latest versions require Python 3.9-3.10
# but Cloud Run runs Python 3.11 via python:3.11-slim. The pipeline
# imports this lazily inside transcribe_piano_hq() and falls back with a
# clear ImportError message if it's missing. Standard quality mode does
# NOT depend on this. To enable HQ piano on a Python 3.10 host, install
# manually: `pip install piano-transcription-inference>=0.0.21`.
# piano-transcription-inference>=0.0.21

# OMR validation (optional — Roboflow). Lazy-imported in omr_validator.py,
# which returns -1.0 (skip) if the package or ROBOFLOW_API_KEY is absent.
# Omitted from the default image to keep the HF build under its timeout.
# Install manually to enable OMR confidence scoring: `pip install inference>=0.9.0`
# inference>=0.9.0
