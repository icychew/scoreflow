"""ScoreFlow per-instrument training and calibration package.

Sub-modules:
    datasets  — download public audio/MIDI datasets (MAPS, GuitarSet, Slakh2100, MIR-1K)
    calibrate — grid-search Basic Pitch thresholds per instrument; writes calibrated_configs.json
"""
